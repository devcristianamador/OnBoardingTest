## 1. Fix Form Validation Feedback

- [ ] 1.1 Add `markAllAsTouched()` call in `BranchFormComponent.onSave()` before the validity check (`fix` commit)

## 2. Unit Tests

- [ ] 2.1 Add unit test verifying that validation errors display when saving an empty form (`test` commit)
- [ ] 2.2 Add unit test verifying that validation errors display when a required field is cleared during edit (`test` commit)

## 3. Verification

- [ ] 3.1 Run full test suite (`ng test`) and verify all tests pass (`test` commit)
